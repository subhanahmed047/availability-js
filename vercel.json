{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npm install && cd docs && npm install && npm run build",
  "installCommand": "npm install && cd docs && npm install",
  "outputDirectory": "docs/.next"
}